<%= form_for(@slice) do |f| %>
  <% if @slice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@slice.errors.count, "error") %> prohibited this slice from being saved:</h2>

      <ul>
      <% @slice.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
